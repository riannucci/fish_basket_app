application: fish-basket
module: ui
version: 1
runtime: go
api_version: go1

instance_class: F1
automatic_scaling:
  min_idle_instances: 1
  max_pending_latency: 30ms
  max_concurrent_requests: 50

handlers:
- url: /static
  static_dir: static
